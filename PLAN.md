# 基于《乱杀法则 1.1》的跑团游戏开发计划

## 一、已确认需求

| 项目 | 决定 |
|---|---|
| 产品形态 | Web 网页版，先做浏览器可玩的线上文字跑团 |
| 单局人数 | 4～7 人；人数不足时可加入 AI 玩家补位 |
| GM 模式 | 同时支持“真人 GM 裁决模式”和“系统自动判定模式” |
| 死亡判定 | 若濒死警告未被全部消除，伤害来源可在自己的回合宣告受害者死亡；玩家也可自认死亡 |
| 规则校验 | 自动校验；系统必须给出判断，GM 模式可作为最终兜底 |
| 限定机制 | 言之有理即可解除；若下一句未能摆脱限定，则其后两句也必须接受该限定 |
| 技术栈 | 后端使用 C 语言，前端使用普通 HTML/CSS/JavaScript |
| 世界状态 | 单局内实时维护；对局结束后保存历史记录（默认方案） |
| 难度分级 | 五档：休闲 / 普通 / 困难 / 噩梦 / 地狱 |

## 〇、当前开发进度（MVP）

- ✅ Web 页面创建/加入房间
- ✅ 4～7 人房间、AI 补位
- ✅ 回合制顺序发言
- ✅ 基础自动规则校验（单句、连词、死者禁创造、限定状态机）
- ✅ 濒死警告与死亡宣告
- ✅ C 语言 HTTP 服务器 + REST API + 前端轮询
- ✅ GM 基础控制台：强制击杀/复活/清除警告/切换回合
- ✅ AI 玩家自动发言（使用同一个 AI 接口生成，失败时模板兜底）
- ✅ 濒死警告 AI 判断（OpenAI 兼容接口，未配置时关键词兜底，延迟处理不阻塞发言）
- ✅ AI 强化审核：合理性、单一谓语、首句离家设定
- ✅ 随机判定模型：权重(难度/合理性/铺垫/操作类型/幸运补偿) + d100
- ✅ 五档难度系统：休闲 / 普通 / 困难 / 噩梦 / 地狱，已接入灾厄积累、AI 救援/致死、随机判定与数值审计
- ✅ 单文件打包：网页资源嵌入 exe，双击启动并自动打开浏览器
- ⏳ 主意识/时间线高级自动判定（可扩展到 LLM）

> **联网现状（升级前）**：目前仍是**本地单进程**应用——HTTP 服务器绑定 `INADDR_ANY`（局域网可访问）、数据全部**内存态**（重启即清空）、前端**轮询**拉取状态、仅靠房间 token 鉴权、AI Key 明文硬编码在 `main.c`（已由 Phase 0 移除）。要变成“真正联网应用”需按下文「八」逐步升级。

## 二、规则理解摘要

- **目标**：玩家每轮通过叙述延续生命，同时设法 kill 其他玩家；最后存活者胜利。
- **核心操作**：
  1. **创造**：创造人物/物品，或使一件事发生。
  2. **扭曲**：引导之前玩家事件走向、存活状态等。
  3. **解释/补充**：让之前的条件更合理，符合作家原则。
- **重要约束**：
  - 顺序发言，不能抢话。
  - 死亡需玩家自认“必死”；不能无故宣告他人死亡。
  - 每轮每人只能说一个“谓语”，不能使用连词，但可用介词。
  - 不能直接操控其他玩家的主意识，但可以“添加其他玩家的遭遇”。
  - 时间不可倒流、不可加速/减缓/定格。
  - 世界规则不受玩家控制。
  - 死者不得再使用“创造”。
- **“限定”技巧**：
  - 例：A“我做了一把枪”→ B“可是！”（限定关键词）→ C“可是枪坏了”。
  - 解除条件：后一句只要“言之有理”、逻辑上能自圆其说，即可摆脱限定。
  - 未解除惩罚：若后一句未能摆脱限定，则其后的两句也必须继续接受该限定。
- **难度分级（已确认 5 档）**：
  - **休闲**：灾厄积累慢，AI 救援倾向较高、主动致死倾向较低，随机判定成功率最高，失败惩罚最轻。
  - **普通**：默认平衡体验，作为其余难度的基准。
  - **困难**：灾厄积累加快，AI 更危险，随机判定成功率下降，失败效果打折扣更明显。
  - **噩梦**：高压力模式，灾厄频繁，AI 救援倾向很低、致死倾向很高，随机判定显著收紧。
  - **地狱**：最高难度，灾厄积累最快，AI 基本不救援且全力追杀，随机判定大幅收紧。
  - 难度仅作为后台隐藏参数参与判定，前端不显示具体数值/权重。

## 三、死亡与濒死机制

1. 当玩家 A 的叙述对玩家 B 造成伤害/致命威胁时，系统记录：
   - `damage_source = A`
   - `victim = B`
   - 生成“濒死警告”。
2. 系统自动向所有玩家展示当前濒死警告；所有玩家有义务提醒濒死者。
3. 玩家 B 可以在后续发言中通过“解释/补充/扭曲”等方式消除濒死警告。
4. 若轮到伤害来源 A 发言时，B 仍有未消除的濒死警告，A 可以在这个回合宣告 B 死亡。
5. 玩家也可以随时自认“必死”并宣告自己的死亡。
6. 死亡后：不能再使用“创造”，但可继续以死者身份发言/参与叙事，具体体验可在后续试玩中调整。

## 四、建议的产品方案

### 1. 核心功能

- 创建/加入房间，支持 4～7 人，人数不足时用 AI 补位。
- 回合制发言：
  - 显示当前轮到谁、还剩谁存活、当前轮次。
  - 玩家发言时可声明操作类型：创造 / 扭曲 / 解释补充 / 限定。
  - 系统记录每轮叙述，形成“叙事链”。
- 系统自动判定：
  - 是否轮到自己、是否还活着。
  - 是否使用“创造”而死者不得使用。
  - 是否违反单句/谓语/连词/介词规则。
  - 是否直接操控其他玩家主意识。
  - 是否触发“限定”，以及是否解除限定。
  - 是否产生濒死警告，以及能否宣告死亡。
- GM 模式：
  - GM 可以看到系统判定，并有权覆盖/裁决争议。
  - 系统判定仍然是第一道关卡。
- 对局结束条件：
  - 只剩 1 名存活玩家时自动结束。
  - 展示完整时间线、胜负记录和死亡过程。

### 2. 技术架构（C 语言后端）

```text
浏览器前端（HTML/CSS/JavaScript）
  ├─ 房间页：创建/加入、人数选择、AI 补位设置
  ├─ 游戏页：发言输入框、回合提示、叙事历史、存活状态、濒死警告
  └─ GM 控制台：裁决、查看系统判定、修改状态

C 语言后端
  ├─ HTTP + WebSocket 服务（建议 mongoose 或 libwebsockets）
  ├─ 房间/会话管理
  ├─ 回合与发言顺序引擎
  ├─ 叙事/世界状态存储
  ├─ 规则校验引擎
  │   ├─ 单句/谓语/连词/介词检查（中文分词 + 规则表）
  │   ├─ 主意识检查
  │   ├─ 时间线检查
  │   └─ 限定状态机
  ├─ 濒死/死亡/胜负判定引擎
  ├─ AI 玩家模块（规则模板 + 可扩展 LLM 接口）
  └─ 数据持久化（SQLite 或 JSON 文件）
```

### 3. 数据模型（初步）

```text
Room
  - id, name, status, min_players=4, max_players=7
  - mode: auto / gm
  - order: [player_ids]
  - current_turn_index
  - round_number
  - created_at

Player
  - id, room_id, name
  - alive: bool
  - is_ai: bool
  - is_ready: bool

Narration
  - id, room_id, round, player_id
  - operation: create / twist / explain / limit
  - content
  - keyword（限定时使用）
  - status: normal / damage / death
  - created_at

NearDeathWarning
  - id, room_id
  - damage_source_id
  - victim_id
  - narrative_id
  - reason
  - resolved: bool
  - created_at, resolved_at

WorldState
  - room_id
  - timeline: [event summaries]
  - characters/items/regions（可选）
  - current_limit:
    - keyword
    - remaining_forced_sentences
```

### 4. 游戏流程

1. 建房 → 设置模式（自动/GM）→ 加入 4～7 人 → 准备开始。
2. 若人数不足 4 人，可选择 AI 玩家补位。
3. 系统随机或由房主指定发言顺序。
4. 每轮按顺序发言：
   - 玩家输入一句话；
   - 标注操作类型；
   - 系统自动检查规则；
   - 通过后写入叙事链并更新世界状态；
   - 若产生濒死警告，系统广播给全员。
5. 若存在未消除的濒死警告，轮到伤害来源时可宣告受害者死亡。
6. 当只剩 1 名存活玩家时，游戏结束。
7. 结束后展示对局回放和胜负记录。

## 五、C 语言后端模块规划

| 模块 | 说明 |
|---|---|
| `server` | HTTP/WebSocket 服务，静态文件服务，客户端消息分发 |
| `session` | 房间、玩家、连接管理 |
| `game` | 回合、顺序、状态机、胜负判定 |
| `narrative` | 叙事链、世界状态、时间线维护 |
| `rules` | 自动规则校验：单句/谓语/连词/主意识/时间/限定 |
| `judge` | 濒死警告、死亡宣告、GM 裁决 |
| `ai` | AI 玩家发言生成，先做模板规则，后续可接 LLM |
| `storage` | SQLite/JSON 持久化 |
| `util` | JSON 解析、字符串处理、中文分词基础工具 |

## 六、开发里程碑

### M0：需求确认（已完成）
- 已确认：Web 版、4～7 人、双模式、自动判定、C 语言后端。
- 下一步：细化规则引擎的判定标准与 UI 原型。

### M1：可玩 MVP
- 搭建 C 语言 HTTP/WebSocket 服务。
- 实现房间、加入、发言顺序、文本聊天、存活状态。
- 前端能做基本的创建/加入/发言/展示历史。
- 先不做复杂 NLP，用规则表做基础检查。

### M2：规则引擎
- 实现自动检查：
  - 是否轮到自己；
  - 是否活着；
  - 死者是否使用创造；
  - 连词/介词/单句基础检查；
  - 限定状态机。
- 实现“限定”的完整流程和未解除惩罚（后两句继续接受限定）。

### M3：死亡与濒死系统
- 实现濒死警告记录、广播、消除、宣告死亡。
- 实现伤害来源在自己的回合宣告受害者死亡。
- 实现 GM 模式的覆盖/裁决功能。

### M4：AI 玩家
- 实现简单规则型 AI：能根据当前叙事生成合理单句。
- 可扩展为调用外部 LLM API。

### M5：打磨与测试
- 4～7 人多人试玩。
- 收集规则争议，补充自动判定规则表。
- 移动端适配、对局回放、UI 美化。

## 七、下一步

1. 我先完成「八」中 **Phase 0（密钥清理）** 与 **Phase 1（SQLite 持久化）** 两件事，并配套一份 **Docker + Nginx/Caddy HTTPS 部署配置**，让项目能立刻部署到公网可玩。
2. 然后按「八」Phase 2 引入实时通信（WebSocket/SSE）改善体验。
3. 账号/回放/移动端属于可选增强，放在最后。

## 八、联网应用升级计划（真·公网可玩）

> 目标：把当前“本地单进程 / 内存态 / 轮询”升级为“公网可访问、数据不丢、可扩展、够安全”的联网应用。
> 原则：**每一阶段都可独立上线**，避免一次大重构。

### 现状差距

| 维度 | 现状 | “真联网”需要 |
|---|---|---|
| 部署 | 本机单进程，双击/命令行 | 公网服务器 / 容器 / HTTPS 域名 |
| 存储 | 全部内存，重启即清空 | SQLite 持久化（房间/叙事/胜负记录） |
| 实时性 | 前端轮询 `/api/game/state` | WebSocket 推送，降低延迟与开销 |
| 身份 | 仅房间 token（存内存） | 持久化账号/会话，Token 可校验 |
| 并发 | 单进程、全局数组、`MAX_ROOMS=64` | 横向扩展或至少支撑更多房间 |
| 安全 | AI Key 明文硬编码、无限流、无 CORS | 密钥放服务端/环境变量、限流、鉴权 |
| 运维 | 无日志、无监控、无健康检查 | 结构化日志、健康端点、优雅退出 |

### Phase 0：安全与密钥（✅ 已完成）

- 删除 `main.c` 中硬编码的 `LUANSHA_AI_KEY`，改为**只从环境变量读取**，避免部署后 API Key 暴露。
- 默认值只保留 URL/模型，Key 必须显式配置；未配置时回退关键词兜底（现有逻辑已支持）。
- 增加**限流**（每 IP 每接口每秒次数）与请求体大小校验，防止被打爆。

### Phase 1：持久化（数据不再丢失）

- 引入 SQLite（C 下用 `sqlite3`；或退而求其次用 JSON 文件落地），替换/兜底内存态：
  - `rooms` / `players` / `narratives` / `warnings` / `world_state` 落库；
  - 启动时加载、写入时落盘；
  - 对局结束后把完整叙事链/胜负记录存为历史（对应上文“保存历史记录”）。
- 服务器重启、进程崩溃都不丢局。

### Phase 2：实时通信

- 增加 WebSocket（`mongoose`/`libwebsockets`，或先做 SSE）推送状态变更，取代高频轮询。
- 保留现有 REST API 作为兼容接口，前端检测到 WebSocket 时自动切换。

### Phase 3：部署与可扩展

- **方案 A（推荐起步）**：单机 Docker 容器 + 反向代理（Nginx/Caddy）上 HTTPS，绑定域名，改动最小，一天内可上线。
- **方案 B（后续）**：把“游戏状态机”与“HTTP 传输”解耦，支持多 worker/多实例 + 共享存储（SQLite WAL 或 Redis）。当前 4～7 人、64 房间规模**没必要一上来就微服务**。
- 加 `/health` 健康检查端点 + 结构化日志，方便接入监控。

### Phase 4：账号与会话（可选）

- 若要长期运营：从“房间 token”升级为注册账号 + 登录态 + 好友/历史战绩。
- 若只是熟人联机：保留房间码 + 邀请链接即可，不必做账号系统。

### Phase 5：体验与稳定性（可选）

- 移动端适配、断线重连（掉线自动挂起/AI 托管）、对局回放、UI 美化。

### 落地优先级

**先公网可访问 + 安全（Phase 0+3A）→ 再用持久化保证连续性（Phase 1）→ 实时通信改善体验（Phase 2）→ 账号/回放/移动端（Phase 4/5）**。
账号系统不应挡住“能玩”这个核心目标。

## 九、客户端化与互联网联机计划（待确认）

> 目标：
> 1. 脱离浏览器，封装成可直接运行的桌面客户端；
> 2. 支持互联网联机，而不仅限局域网。

### 9.1 客户端化

#### 目标
- 玩家不再需要手动打开浏览器，双击客户端即可进入游戏；
- 客户端内置本地 HTTP 服务 + 前端页面，自动拉起并显示游戏窗口。

#### 方案对比

| 方案 | 说明 | 成本 | 风险 |
|---|---|---|---|
| A. WebView2 内嵌（推荐） | Windows 自带 WebView2，客户端启动本地服务后加载 `http://127.0.0.1:port` | 低-中 | 依赖用户系统 WebView2 Runtime（Win10/11 基本自带） |
| B. Tauri/Electron 包装 | 用前端壳包住现有 web 资源 | 中-高 | 引入 Node/Rust 工具链，项目变复杂 |
| C. 原生 Win32 UI | 完全自绘界面 | 高 | 工作量大，不划算 |

#### 推荐路径
- 采用 **WebView2 内嵌**：
  1. 保留现有 `web/` 前端与 C HTTP 服务；
  2. 新增客户端入口（如 `client/win/main.c`），创建隐藏控制台窗口 + WebView2 窗口；
  3. 客户端自动启动本地 HTTP 服务，WebView 加载本地地址；
  4. 支持“启动即建房/快速加入房间码”入口，减少操作步骤；
  5. 保留 `--no-webview` 或 `--browser` 参数，退回浏览器模式。

### 9.2 互联网联机

#### 目标
- 不同网络下的玩家可通过一个公网地址加入游戏。

#### 方案对比

| 方案 | 说明 | 成本 | 风险 |
|---|---|---|---|
| A. 内置 Cloudflare Tunnel（推荐） | 客户端自动拉起 `cloudflared`，生成临时公网 HTTPS 地址 | 低 | 免费临时地址每次变化；需分发 `cloudflared.exe` |
| B. 内置 cpolar/ngrok | 国内/国外可用，但需要用户账号与 token | 中 | 配置门槛高，不适合一键开玩 |
| C. 自建 frp/云服务器 | 固定地址、稳定 | 高 | 需要额外服务器和运维 |

#### 推荐路径
- 第一阶段内置 **Cloudflare Tunnel**：
  1. 项目内附带 `cloudflared.exe`（或首次启动时提示下载）；
  2. 客户端启动本地服务后，可选“开启互联网联机”；
  3. 自动运行 `cloudflared tunnel --url http://127.0.0.1:端口`；
  4. 从输出中解析 `https://xxx.trycloudflare.com` 并显示在客户端界面；
  5. 玩家复制该地址发给朋友即可加入。
- 第二阶段支持自定义穿透命令：
  - 允许用户配置自己的 `ngrok` / `cpolar` / `frp` 命令，客户端负责拉起和展示地址。

### 9.3 实施阶段

#### Step 1：客户端壳
- 新增 WebView2 客户端入口；
- 双击启动，自动打开本地游戏窗口；
- 保留浏览器模式。

#### Step 2：一键互联网联机
- 集成 `cloudflared`；
- 客户端界面增加“开启公网联机”按钮；
- 显示公网地址、复制按钮、关闭隧道按钮。

#### Step 3：体验优化
- 启动自检（端口占用、WebView2 是否可用、cloudflared 是否存在）；
- 断线提示与重连；
- 打包脚本（生成可分发的 exe + 依赖）。

### 9.4 优先级建议
1. 先做客户端壳（不依赖穿透，可独立发布）；
2. 再内置 cloudflared 一键公网；
3. 最后做自定义穿透与打包优化。

### 9.5 风险与注意
- WebView2 在旧 Windows 上可能缺失，需提供检测与安装引导；
- cloudflared 免费隧道地址随机，适合熟人临时联机，不适合长期固定入口；
- 如果目标用户都在国内，Cloudflare 隧道可能不稳定，需保留 cpolar 自定义方案。
